package io.github.guillermodubon.coachgym.organization.application;

/** Allowlisted fields available for the branch catalog ordering. */
public enum GymBranchSortField {
    CODE,
    NAME,
    CITY,
    CREATED_AT,
    UPDATED_AT
}
