package io.github.guillermodubon.coachgym.client;

/** Allowlisted fields available for client search ordering. */
public enum ClientSortField {
    CLIENT_CODE,
    FIRST_NAME,
    LAST_NAME,
    CREATED_AT,
    UPDATED_AT
}
